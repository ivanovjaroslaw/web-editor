const firebase = {
  apiKey: '',
  authDomain: '',
  databaseURL: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
};

const form = {
  inputDebounceInterval: 500,
};

const config = { firebase, form };

export default config;
